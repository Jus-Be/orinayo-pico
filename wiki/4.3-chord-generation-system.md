# Chord Generation System

> **Relevant source files**
> * [main.c](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/main.c)
> * [pico_bluetooth.c](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c)

## Purpose and Scope

This document describes the chord generation system in `pico_bluetooth.c`, which translates guitar controller fret button combinations into MIDI chord notes. The system uses a lookup table (`chord_chat`) to map guitar fingering patterns to actual note values, supports multiple chord types (major, minor, suspended), and adapts output based on neck position and strum pattern selections.

For information about how button states are captured from the controller, see [HID to MIDI Translation](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/HID to MIDI Translation)

 For details on the different operational modes that consume chord data, see [Operational Modes](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/Operational Modes)

---

## System Overview

The chord generation system operates as a multi-stage pipeline that converts button combinations into precise MIDI note sequences:

```mermaid
flowchart TD

Input["Fret Buttons<br>(green/red/yellow/blue/orange)"]
Detect["Chord Detection Logic<br>play_chord()"]
Type["Chord Type Determination<br>(major/minor/sus)"]
Lookup["chord_chat Table Lookup<br>[12][3][6]"]
NeckPos["Neck Position Application<br>(active_neck_pos: 1-3)"]
StrumSel["Strum Pattern Selection<br>(active_strum_pattern: -1 to 4)"]
NoteGen["MIDI Note Generation<br>string_frets[] + chord_chat[]"]
Output["MIDI Note Output<br>midi_send_note()"]

Input --> Detect
Detect --> Type
Type --> Lookup
Lookup --> NeckPos
NeckPos --> StrumSel
StrumSel --> NoteGen
NoteGen --> Output
```

**Sources:** [pico_bluetooth.c L1401-L1836](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1401-L1836)

---

## Chord Detection Logic

The `play_chord()` function maps button combinations to specific chords. The system recognizes 31 different chord fingerings, including slash chords and suspended chords.

### Button Combination Mappings

The following table shows the primary chord mappings used in `play_chord()`:

| Buttons | Chord | Notes (relative to base) | Chord Type | seqtrak_chord |
| --- | --- | --- | --- | --- |
| yellow | C | base, base+4, base+7 | Major (0) | 1 |
| blue | Dm | base+2, base+5, base+9 | Minor (1) | 2 |
| orange | F | base-7, base+9, base+12 | Major (0) | 4 |
| green | G | base-5, base+11, base+14 | Major (0) | 5 |
| red | Am | base-3, base+12, base+16 | Minor (1) | 6 |
| red+blue | D | base+2, base+6, base+9 | Major (0) | 0 |
| green+red | G/B | base-13, base+7, base+11, base+14 | Slash | 5 |
| green+blue | Em | base-8, base+7, base+11 | Minor (1) | 3 |
| yellow+blue | C/E | base-20, base, base+4, base+7 | Slash | 1 |
| green+yellow | Gsus | base-5, base+12, base+14 | Sus (2) | 5 |
| orange+yellow | Csus | base, base+5, base+7 | Sus (2) | 1 |
| red+yellow | Bb | base-2, base+2, base+5 | Major (0) | 7 |

Complex four-button combinations support additional slash chords like F/C, G/C, and others.

**Sources:** [pico_bluetooth.c L1401-L1633](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1401-L1633)

### Chord Detection Flow

```mermaid
flowchart TD

Start["Button State Input"]
Check4["Check 4-Button Combos"]
Check3["Check 3-Button Combos"]
Check2["Check 2-Button Combos"]
Check1["Check Single Buttons"]
SetParams["Set chord_note, chord_type, bass_note"]
UpdateLast["Update last_chord_note, last_chord_type"]
GenerateNotes["Generate MIDI Notes"]

Start --> Check4
Check4 --> SetParams
Check4 --> Check3
Check3 --> SetParams
Check3 --> Check2
Check2 --> SetParams
Check2 --> Check1
Check1 --> SetParams
Check1 --> GenerateNotes
SetParams --> UpdateLast
UpdateLast --> GenerateNotes
```

**Sources:** [pico_bluetooth.c L1401-L1654](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1401-L1654)

---

## The chord_chat Lookup Table

The `chord_chat` table is a three-dimensional array that stores guitar fingering positions: `[12][3][6]`, representing `[root note][chord type][string]`.

### Table Structure

```
int chord_chat[12][3][6]
```

* **Dimension 1 (12):** Root note (0=C, 1=C#/Db, 2=D, ..., 11=B) [pico_bluetooth.c L231-L244](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L231-L244)
* **Dimension 2 (3):** Chord type (0=Major, 1=Minor, 2=Suspended) [pico_bluetooth.c L231-L244](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L231-L244)
* **Dimension 3 (6):** Guitar string (0=6th/low E, 5=1st/high E) [pico_bluetooth.c L231-L244](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L231-L244)

Each value represents the fret number to press on that string, or `-1` if the string is not played.

### Example: C Major Chord

```
chord_chat[0][0] = {3, 3, 2, 0, 1, 0}
```

This represents:

* String 6 (E): Fret 3 (G)
* String 5 (A): Fret 3 (C)
* String 4 (D): Fret 2 (E)
* String 3 (G): Fret 0 (G)
* String 2 (B): Fret 1 (C)
* String 1 (E): Fret 0 (E)

### Chord Type Index Mapping

| Index | Type | Description |
| --- | --- | --- |
| 0 | Major | Standard major triad |
| 1 | Minor | Minor triad (flattened 3rd) |
| 2 | Suspended | Suspended 4th chord |

**Sources:** [pico_bluetooth.c L231-L244](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L231-L244)

---

## Neck Positions and Octave Ranges

The `active_neck_pos` variable (1-3) controls the octave range of generated notes, simulating different positions on a guitar neck.

### Neck Position Configuration

```mermaid
flowchart TD

LowPos["active_neck_pos = 1<br>Bass/Low Position"]
NormPos["active_neck_pos = 2<br>Normal Position"]
HighPos["active_neck_pos = 3<br>High Position"]
String6["String 6: E3 (40)"]
String6N["String 6: E4 (52)"]
String6H["String 6: E5 (64)"]

LowPos --> String6
NormPos --> String6N
HighPos --> String6H
```

### String Frequency Calculation

The base string frequencies are calculated in `play_chord()`:

```
int O = 12;  // Octave offset (12 semitones)int E = 4, A = 9, D = 2, G = 7, B = 11;  // Note values int __6th = E + O*(active_neck_pos+2);  // Low E stringint __5th = A + O*(active_neck_pos+2);  // A stringint __4th = D + O*(active_neck_pos+2);  // D stringint __3rd = G + O*(active_neck_pos+2);  // G stringint __2nd = B + O*(active_neck_pos+2);  // B stringint __1st = E + O*(active_neck_pos+3);  // High E string (one octave higher)
```

### MIDI Note Calculation

The final MIDI note for each string is calculated as:

```
MIDI_note = string_base_frequency + chord_chat[chord_note % 12][chord_type][string_index]
```

**Example:** For C major at normal position (`active_neck_pos=2`):

* String 6: 52 (E4) + 3 = 55 (G4)
* String 5: 57 (A4) + 3 = 60 (C5)
* String 4: 50 (D4) + 2 = 52 (E4)

**Sources:** [pico_bluetooth.c L123](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L123-L123)

 [pico_bluetooth.c L1635-L1649](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1635-L1649)

---

## Strum Pattern System

The `strum_pattern` array defines which strings to play in what order for different playing styles.

### Strum Pattern Array Structure

```
uint8_t strum_pattern[14][12][6]
```

* **Dimension 1 (14):** Pattern type [pico_bluetooth.c L246](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L246-L246)
* **Dimension 2 (12):** Sequence step [pico_bluetooth.c L246](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L246-L246)
* **Dimension 3 (6):** String play order [pico_bluetooth.c L246](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L246-L246)

### Pattern Types

| active_strum_pattern | Description | Use Case |
| --- | --- | --- |
| -1 | No chord data | Pattern-based arpeggiator |
| 0 | Full strum | All 6 strings in sequence |
| 1 | Strum with bass | 4 strings, bass emphasis |
| 2 | Arpeggio up | Individual string picking |
| 3 | Arpeggio down | Reverse picking |
| 4 | Complex arpeggio | Advanced picking pattern |

### Pattern Decoding Logic

```mermaid
flowchart TD

Step["strum_pattern[pattern][seq_index][i]"]
Decode["string = 6 - pattern_value"]
Lookup["chord_chat[chord_note][chord_type][string]"]
CalcNote["MIDI_note = string_frets[string] + fret_value"]
Filter["Skip if fret_value == -1"]

Step --> Decode
Decode --> Lookup
Lookup --> Filter
Filter --> CalcNote
```

### Example: Pattern 0 (Full Strum)

```python
strum_pattern[0][0] = {6,5,4,3,2,1}  // Play all strings from 6th to 1st
```

The system iterates through this pattern, converting each value to a string index:

* `6 - 6 = 0` → String 6 (low E)
* `6 - 5 = 1` → String 5 (A)
* `6 - 4 = 2` → String 4 (D)

**Sources:** [pico_bluetooth.c L246](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L246-L246)

 [pico_bluetooth.c L1676-L1696](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1676-L1696)

---

## Note Generation and Velocity Dynamics

The system generates MIDI notes with dynamic velocity based on strum direction and position.

### Velocity Calculation

```mermaid
flowchart TD

Start["Initial velocity = 110"]
Sort["Strum Direction?"]
Up["Sort notes ascending<br>compUp()"]
Down["Sort notes descending<br>compDown()"]
Loop["For each note"]
Reduce["velocity -= 10<br>(min: 25)"]
Send["midi_send_note()"]

Start --> Sort
Sort --> Up
Sort --> Down
Up --> Loop
Down --> Loop
Loop --> Send
Send --> Reduce
Reduce --> Loop
```

### Special Note Handling

**Bass Note Generation:**
When `enable_midi_drums` is active and a downstroke occurs, a bass note is triggered on MIDI channel 1 (0x90) using program 33 (Bass).

```
if (!up && enable_midi_drums && active_strum_pattern == 0) {    note = ((bass_note ? bass_note : chord_note) % 12) + (O * (active_neck_pos + 1));    if ((note % 12) > 4) note = note - 12;  // Lower octave for bass        midi_send_program_change(0xC0, 33);  // Bass program    midi_send_note(0x90, note, 120);    midi_send_program_change(0xC0, guitar_pc_code);  // Restore guitar}
```

**Ample Guitar Compatibility:**
When `enable_ample_guitar` is active, notes below MIDI 40 are transposed up one octave to fit the instrument's range.

```
if (enable_ample_guitar && note < 40) note = note + 12;
```

**Sources:** [pico_bluetooth.c L1698-L1741](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1698-L1741)

 [pico_bluetooth.c L1710-L1714](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1710-L1714)

 [pico_bluetooth.c L1726-L1737](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1726-L1737)

---

## Integration with Other Systems

### Chord Data Consumers

```mermaid
flowchart TD

ChordGen["Chord Generation<br>play_chord()"]
AmpleGuitar["Ample Guitar Mode<br>Squeeze to C1-B2<br>enable_ample_guitar"]
SeqTrak["SeqTrak Sampler<br>Chord triggers<br>seqtrak_chord + 59"]
MODX["MODX Arpeggiator<br>Note compression<br>enable_modx"]
Arranger["Arranger Mode<br>Channel 4 pads<br>enable_arranger_mode"]
Compress["MIDI notes % 12 + 36"]
Trigger["0x9A channel 11"]

ChordGen --> AmpleGuitar
ChordGen --> SeqTrak
ChordGen --> MODX
ChordGen --> Arranger
AmpleGuitar --> Compress
SeqTrak --> Trigger
MODX --> Compress
```

**Sources:** [pico_bluetooth.c L1401-L1836](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1401-L1836)

 [pico_bluetooth.c L55-L57](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L55-L57)

### State Variables

The system maintains state through several global variables in `pico_bluetooth.c`:

| Variable | Type | Purpose |
| --- | --- | --- |
| `last_chord_note` | `int` | Last detected chord root note (0-11) |
| `last_chord_type` | `int` | Last chord type (0=maj, 1=min, 2=sus) |
| `active_neck_pos` | `int` | Current neck position (1-3) |
| `active_strum_pattern` | `int` | Current strum pattern (0-13) |
| `old_midinotes[6]` | `uint8_t[]` | Previously played notes for note-off |
| `chord_notes[6]` | `uint8_t[]` | Current chord notes for playback |

**Sources:** [pico_bluetooth.c L129-L130](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L129-L130)

 [pico_bluetooth.c L123-L124](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L123-L124)

 [pico_bluetooth.c L173-L174](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L173-L174)

---

## Summary

The chord generation system provides a sophisticated mapping from guitar controller buttons to MIDI note sequences. The `chord_chat` lookup table enables realistic guitar voicings, while the strum pattern system and neck position controls offer flexibility in performance style. The integration with external synthesizers like SeqTrak and MODX allows for automated accompaniment based on detected fingerings.

**Sources:** [pico_bluetooth.c L1-L1836](https://github.com/Jus-Be/orinayo-pico/blob/6dde5a75/pico_bluetooth.c#L1-L1836)